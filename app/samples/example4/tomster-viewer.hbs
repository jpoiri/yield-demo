<div class="panel panel-default">
    {{#if hasBlock}}
        <div class="panel-heading">{{@heading}}</div>

        {{yield (hash theme=theme id=elementId)}}

    {{else}}
        <div class="panel-heading">{{@heading}}</div>

        <Tomster @theme={{@theme}} />

    {{/if}}
</div>